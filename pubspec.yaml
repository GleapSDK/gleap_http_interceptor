name: gleap_http_interceptor
description: A Http Interceptor for Gleap
version: 1.2.3
homepage: https://gleap.io

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  http_interceptor: '>=1.0.2 <2.0.0'
  gleap_sdk: '>=6.4.4 <7.0.0'
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: